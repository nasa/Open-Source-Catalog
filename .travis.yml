matrix:
  include:
    - language: python
      python:
        - "2.7"
      script:
        - python test.py
    - language: node_js
      node_js:
        - 'node'
      install:
        - npm install
      sudo: false
notifications:
  email:
    - evantayloryates@gmail.com